# =============================================================================
# Cora AI — Python Dependencies
# =============================================================================
# Single requirements file for the full local-first stack:
#   - Running the FastAPI server + React SPA
#   - Document ingestion (Document Store PDF conversion)
#   - Testing is in requirements-dev.txt
#
# Dev-only deps (RAGAS evaluation, load testing) live in requirements-dev.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Usage:
#   pip install -r requirements.txt
# =============================================================================

# Core AI/ML
qdrant-client>=1.18.0,<2.0
google-genai>=2.9.0,<3.0
voyageai>=0.4.1,<0.5
langchain>=1.3.10,<2.0
langchain-community>=0.4.2,<0.5
langchain-core>=1.4.8,<2.0
langchain-qdrant>=1.1.0,<2.0
langchain-voyageai>=0.3.3,<0.4

# Web Framework
fastapi>=0.138.0,<0.140
uvicorn>=0.49.0,<0.51
httpx>=0.28.1,<0.29
certifi>=2026.6.17
# starlette: pinned to fix 6 CVEs in 0.50.0 (FastAPI allows >=0.46.0 which
# includes the vulnerable 0.50.0; pin >=1.3.1 to force the fix on fresh installs)
starlette>=1.3.1,<2.0

# Data Processing
pydantic>=2.13.4,<3.0
pydantic-settings>=2.14.2,<3.0
numpy>=2.4.0,<3.0
pandas>=3.0.3,<4.0
PyYAML==6.0.3

# Document Processing (ingestion)
markdown>=3.10.2,<4.0
python-frontmatter>=1.3.0,<2.0
beautifulsoup4>=4.15.0,<5.0
langchain-text-splitters>=1.1.2,<2.0
tiktoken>=0.13.0,<0.14
python-magic==0.4.27
python-multipart>=0.0.20,<0.1
# PDF conversion:
#   - Docling: standard/classical non-VLM pipeline (layout + RapidOCR + tables)
#   - PyMuPDF: page rendering for llm_api input only
#   - torch/torchvision: Docling 2.107+ imports these unconditionally even
#     when VLM features are off. CPU-only wheels (123MB + 4MB) avoid the 451MB
#     CUDA build. Install with: pip install torch torchvision --index-url
#     https://download.pytorch.org/whl/cpu --no-deps
# Pinned per the 7-day release rule (2.108.0 is <7 days old at pin time).
docling[rapidocr]>=2.106.0,<2.108.0
torch>=2.11.0,<3.0
torchvision>=0.26.0,<1.0
pymupdf>=1.24.0,<2.0

# Authentication
PyJWT[crypto]>=2.13.0,<3.0

# Utilities
python-dotenv==1.2.2
orjson>=3.11.9,<4.0
tenacity>=9.1.4,<10.0
loguru==0.7.3
cachetools>=7.1.4,<8.0
nh3>=0.3.5,<0.4
filelock>=3.29.4,<4.0
zstandard>=0.25.0

# OpenAI embedding/LLM provider (lazy-imported by src/ when EMBEDDING_PROVIDER=openai
# or as a fallback LLM client; not needed for default Voyage/Gemini config)
langchain-openai>=1.3.2,<2.0
openai>=2.43.0,<3.0

# --- Security: pin critical transitive deps to fix CVEs ---
aiohttp>=3.14.1,<4.0          # 30 CVEs in 3.11.8 (request smuggling, RCE, DoS)
cryptography>=48.0.1,<50.0     # 5 CVEs in 44.0.0 (OpenSSL, key validation)
urllib3>=2.7.0,<3.0           # 6 CVEs in 2.3.0
requests>=2.33.0,<3.0         # 1 CVE in 2.32.5
protobuf>=5.29.6               # 1 CVE in 5.29.5; let google packages pull compatible max
jinja2>=3.1.6,<4.0             # 1 CVE in 3.1.5 (sandbox escape)
pillow>=12.2.0,<13.0           # 6 CVEs in 10.4.0 (transitive via voyageai/ragas)
authlib>=1.7.1,<2.0            # 6 CVEs in 1.6.6 (transitive)

